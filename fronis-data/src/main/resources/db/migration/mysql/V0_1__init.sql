CREATE TABLE t_player
(
    pk_player_id            INT(10) NOT NULL,
    first_name    VARCHAR(100) NOT NULL,
    last_name     VARCHAR(100) NOT NULL,
    player_number INT(10),
    PRIMARY KEY (pk_player_id)
);